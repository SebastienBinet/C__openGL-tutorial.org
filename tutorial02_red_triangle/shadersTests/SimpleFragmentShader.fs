#version 330 core

// Ouput data
out vec3 color;

vec3 tempSB;
//float tempInitSB =random(0.0, 0.9); //0.5;

void main()
{
    //tempInitSB = tempInitSB + 0.01;
    //tempSB = vec3(tempInitSB,0.5,0.5);
    
	// Output color = red
    //color = tempSB; //vec3(1,0,0);
    color = vec3(1,0,0);

}