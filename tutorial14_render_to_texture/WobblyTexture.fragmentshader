#version 330 core

in vec2 UV;

out vec3 color;

uniform sampler2D renderedTexture;
uniform float time;

float time2 = 0;

void main(){
    color = texture( renderedTexture, UV + 0.05*vec2( sin(0.05*time*1024.0*UV.x),cos(0.05*time*768.0*UV.y)) ).xyz ;
}